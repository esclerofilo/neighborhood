extends layout

block content
  h1 Hackathon Starter
  p.lead A boilerplate for Node.js web applications.
  hr
  .row
    .col-md-6
      h2
      each val in user.profile.tags
        p
        a.btn.btn-raised(href='/', role='button') #{val}